#pragma once
/*
template<class T>
class Node {
public:
	T value;
	Node<T>* next;
	Node();
	Node(T val);
};
template<class T>
class Linkedlist
{
private:
	Node* head;
	Node* tail;
	int count;
public:
	Linkedlist();
	T At(int pos);
	void Append(T);
	void DeleteAt(int);
};

template<class T>
inline Node<T>::Node()
{
	value = 0;
	next = 0;
}

template<class T>
inline Node<T>::Node(T val)
{
	value = val;
	next = 0;
}
*/
